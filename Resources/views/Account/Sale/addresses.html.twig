{% import '@EkynaCommerce/Account/Sale/macros.html.twig' as macros %}

{% spaceless %}
<div class="row">
    <div class="col-md-6">
        <p>
            <strong>
                {{- 'ekyna_commerce.sale.field.invoice_address'|trans -}}
            </strong>
        </p>
        <div class="panel panel-default">
            <div class="panel-body">
                <p id="account-sale-invoice">{{ sale.invoiceAddress|address }}</p>
                {% if sale is sale_quote and sale.editable -%}
                <p><a href="{{ path('ekyna_commerce_account_quote_invoice_address', {'number': sale.number}) }}"
                      id="account-sale-invoice-button" class="btn btn-xs btn-primary" data-account-modal>
                    {{- 'ekyna_core.button.edit'|trans -}}
                </a></p>
                {%- endif %}
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <p>
            <strong>
                {{- 'ekyna_commerce.sale.field.delivery_address'|trans -}}
            </strong>
        </p>
        <div class="panel panel-default">
            <div class="panel-body">
                <p id="account-sale-delivery">{{ macros.sale_delivery_address(sale) }}</p>
                {% if sale is sale_quote and sale.editable -%}
                <p><a href="{{ path('ekyna_commerce_account_quote_delivery_address', {'number': sale.number}) }}"
                      id="account-sale-delivery-button" class="btn btn-xs btn-primary" data-account-modal>
                    {{- 'ekyna_core.button.edit'|trans -}}
                </a></p>
                {%- endif %}
            </div>
        </div>
    </div>
</div>
{% endspaceless %}
