{% extends '@EkynaCommerce/Account/layout.html.twig' %}

{% block account %}
{% apply spaceless %}
    <h3>{{ 'account.quote.voucher'|trans({'%number%': quote.number}, 'EkynaCommerce')|raw }}</h3>

    <hr>

    {{ bootstrap_set_style('horizontal') }}
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="form-body">
        {{ form_errors(form, {'global_errors': true}) }}
        {{ form_widget(form) }}
        {{ form_rest(form) }}
    </div>
    {{ form_end(form) }}

    <br><br>
    <h3>{{ 'account.quote.title'|trans({}, 'EkynaCommerce')|raw }}</h3>
    <hr>
    {% include '@EkynaCommerce/Account/Quote/_list.html.twig' with {
        'quotes': quotes, 'customer': customer
    } only %}

{% endapply %}
{% endblock %}
