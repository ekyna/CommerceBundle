<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>{{ 'ekyna_core.field.file'|trans }}</th>
            <th>{{ 'ekyna_core.field.title'|trans }}</th>
            <th>{{ 'ekyna_core.field.date'|trans }}</th>
            <th>{{ 'ekyna_commerce.attachment.field.internal'|trans }}</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for attachment in sale.attachments %}
            {% set attachmentId = 'attachment_' ~ loop.index0 %}
            <tr data-id="{{ attachment.id }}">
                <td id="{{ attachmentId }}_filename">{{ attachment }}</td>
                <td id="{{ attachmentId }}_title">{{ attachment.title }}</td>
                <td id="{{ attachmentId }}_updatedAt">{{ attachment.updatedAt|localizeddate('short', 'none') }}</td>
                <td id="{{ attachmentId }}_internal">
                    {% if attachment.internal %}
                        <span class="label label-success">
                            {{ 'ekyna_core.value.yes'|trans }}
                        </span>
                    {% else %}
                        <span class="label label-danger">
                            {{ 'ekyna_core.value.no'|trans }}
                        </span>
                    {% endif %}
                </td>
                <td class="actions">
                    <a href="{{ admin_resource_path(attachment, 'download') }}" class="btn btn-primary btn-xs" target="_blank">
                        {{ 'ekyna_core.button.download'|trans }}
                    </a>
                    <a href="{{ admin_resource_path(attachment, 'edit') }}" class="btn btn-warning btn-xs">
                        {{ 'ekyna_core.button.edit'|trans }}
                    </a>
                    <a href="{{ admin_resource_path(attachment, 'remove') }}" class="btn btn-danger btn-xs">
                        {{ 'ekyna_core.button.remove'|trans }}
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8" style="text-align: center">
                    <em>{{ 'ekyna_commerce.attachment.alert.no_item'|trans }}</em>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
