{% extends "EkynaAdminBundle:Entity/Default:new.html.twig" %}

{% block title %}
    <h1>{{ 'ekyna_commerce.sale.header.item.add'|trans|raw }}</h1>
{% endblock title %}

{% block actions %}
    {#{{ admin_resource_btn(resource, 'list') }}
    {{ admin_resource_btn(resource, 'new') }}#}
{% endblock actions %}

{% block main %}
    {{ form_start(form) }}
    <div class="form-body">
        {{ form_widget(form) }}
        {{ form_rest(form) }}

        {% include 'EkynaAdminBundle:Form:form_flow_buttons.html.twig' %}
    </div>
    {{ form_end(form) }}
{% endblock main %}
