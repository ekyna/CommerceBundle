{% extends '@EkynaAdmin/Entity/Crud/_form_default.html.twig' %}

{% block main_tabs -%}
    <li class="active">
        <a href="#{{ resource_name }}-general" id="toggle-general" data-toggle="tab">
            {{ 'field.general'|trans({}, 'EkynaUi') }}
        </a>
    </li>
{%- endblock main_tabs %}

{% block main_contents -%}
    <div class="tab-pane active" id="{{ resource_name }}-general">
        {{ form_row(form.subjectIdentity) }}
        {{ form_row(form.components) }}
        <div class="row">
            <div class="col-md-6">
                {{ form_row(form.cost, {'label_col': 4, 'widget_col': 8}) }}
            </div>
        </div>
        {{ form_row(form.description) }}
    </div>
{%- endblock main_contents %}
