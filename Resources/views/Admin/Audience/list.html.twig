{% extends '@EkynaAdmin/Entity/Crud/list.html.twig' %}

{#{% block actions %}
    {{ admin_resource_btn(resource_id, 'synchronize', {
        'icon': 'refresh'
    }) }}
    {{ admin_resource_btn(resource_id, 'list') }}
{% endblock %}#}
