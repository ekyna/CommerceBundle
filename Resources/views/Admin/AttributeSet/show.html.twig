{% extends "EkynaAdminBundle:Entity/Default:show.html.twig" %}

{% block main %}
<div class="actions">
    {{ admin_resource_btn(attributeSet, 'edit') }}
    {{ admin_resource_btn(attributeSet, 'remove') }}
</div>
<div class="show show-with-tabs">
    <ul class="nav nav-tabs">
        <li class="active">
            <a href="#general" data-toggle="tab">{{ 'ekyna_core.field.general'|trans }}</a>
        </li>
    </ul>
    <div class="show-body">
        <div class="tab-content">
            <div class="tab-pane active" id="general">
                {{ show_row(attributeSet.name, 'text', 'ekyna_core.field.email') }}
                {{ show_row(attributeSet.slots, 'entity', 'ekyna_commerce.attribute_group.label.plural', {
                    'field': 'group.name',
                    'route': 'ekyna_commerce_attribute_group_admin_show',
                    'route_params_map': {'attributeGroupId': 'id'},
                }) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
