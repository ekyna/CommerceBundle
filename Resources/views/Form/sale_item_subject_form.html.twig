{{ bootstrap_set_style('horizontal') }}
{{ bootstrap_set_col_size('xs') }}

{{ form_errors(form) }}

{% if form.configuration is defined %}
    {% form_theme form.configuration 'EkynaCommerceBundle:Form:configurable_slot.html.twig' %}
    {{ form_widget(form.configuration) }}
{% endif %}

{% if form.quantity is defined %}
    {{ form_row(form.quantity) }}
{% endif %}

{{ form_rest(form) }}
